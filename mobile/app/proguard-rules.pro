# Project-specific ProGuard rules.
# AGP ships sensible defaults via proguard-android-optimize.txt; add custom rules here as needed.

# kotlinx.serialization keeps generated serializers via its own consumer rules.
# Compose, Hilt, and AndroidX components all ship consumer rules and need no extra config here.
